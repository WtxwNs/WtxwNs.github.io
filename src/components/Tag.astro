---
interface Props { label: string }
const { label } = Astro.props;
---
<span class="inline-flex rounded-full border border-[var(--line)] px-2.5 py-1 text-xs font-medium text-[var(--muted)]">{label}</span>
